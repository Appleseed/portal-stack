version: '2.1'
services:
  db:
    build:
     context: .
     #Testing direct file for now
     dockerfile: portal-stack/docker/sql/Dockerfile
    environment:
      sa_password: "Password1"
    ports:
      - "1433:1433" # REMARK: This is currently required, needs investigation

  web:
    build:
      context: .
      # Testing direct file for now
      dockerfile: portal-stack/docker//portal/windows/Dockerfile
    environment:
     # - "Data:DefaultConnection:ConnectionString=Server=db,1433;Database=MusicStore;User Id=sa;Password=Password1;MultipleActiveResultSets=True"
    depends_on:
      - "db"
    ports:
      - "80:80"

networks:
  default:
    external:
      name: nat
